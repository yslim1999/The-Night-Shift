label instruction:
    scene instruction with fade

    $ renpy.pause(2.0, hard = True)

    show screen instruLeave

    while start is not True:
        $ renpy.pause(1.0, hard = True)

    return
